# 更新记录

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## v1.0.0 — 2026-09-14

首个可用版本：从"设计文档阶段"落地为可在内网单机运行的完整系统。

### 模板与字段

- 模板中心：新建模板 → 创建版本 → 上传 DOCX 母版 → 字段配置 → 校验 → 试填 → 确认试填 → 发布。
- 按**发布式版本**管理：已发布版本不可原地修改；版本号自动分配并复用被删除版本腾出的号；版本可命名。
- 占位符协议 `{{key}}`，键名 `^[a-z][a-z0-9_]{0,63}$`；支持跨 run 的占位符（要求样式一致），替换时保留原格式（含下划线）。
- 母版无占位符时自动识别：指定颜色标记的文字、连续 ≥3 个下划线；改写为占位符作为渲染母版，原始上传件单独归档。
- **字段配置与母版自动对齐**：上传母版时自动补新增字段、删已移除字段，并修复被删字段弄坏的引用（金额大写来源、日期先后比较、分组归属）；另提供【按母版同步字段】手工触发。
- **字段默认值**：字段配置里的 `default` 在新建合同时预填进草稿（计算字段除外）。
- 字段类型：文本、多行、日期（正文按中文输出）、金额（两位小数 + 区间校验）、整数、下拉、是否、金额大写（`rmb_upper` 自动计算）。
- 上传安全检查：拒绝宏/嵌入对象/自定义 XML、外链、修订痕迹、文本框/内容控件/域中的占位符、纵向合并单元格中的占位符；限制解压体积与文件数量。

### 合同

- 在线填报（按类型渲染控件）、草稿保存、全文生成（Word 由母版替换、PDF 由 LibreOffice 转换）、查看全文 PDF → 确认全文 → 定稿、下载、签署件归档（可补传/作废）、作废、派生新草稿、模板升级与数据迁移。
- **合同编号**改为按"当年已用编号里的最小空位"分配（不再使用数据库序列）：删空回到 `000001`，删中间则补空位。
- 合同/版本/模板支持**物理删除**（草稿本人可删、已作废仅管理员；模板被合同引用时拒绝删除），审计记录保留。

### 安全与运维

- BCrypt 密码哈希、会话 Cookie、CSRF 令牌、按账户与来源双重登录失败限速、首次登录强制改密、弱密码黑名单（长度下限按业务要求设为 6 位）。
- 首个管理员只能通过命令行创建（`--init-admin`），另提供 `--recover-admin`；无网页初始化入口。
- 审计日志追加式保留：模板/合同/账号全生命周期动作，含删除原因与合同编号。
- 私有文件存储 + 孤儿文件回收（默认保留 24 小时）；渲染指纹 + 确认凭据保证"试填/定稿"绑定的母版与字段不可被悄悄替换。
- PostgreSQL 16 为生产目标（Flyway `V1`～`V5`），内嵌 H2 文件库用于零配置体验与自动化测试。
- Windows 一键启动脚本：Docker 中启动 PostgreSQL → 等待数据库**真正可连接** → 启动 jar → 健康检查 → 打开浏览器。

### 界面

- 零构建原生 JS 单页应用：工作台、我的合同、模板中心、账号管理、审计日志、系统状态。
- 模板「字段配置」弹窗内置"填充位置核对表"（每个字段在母版里出现几处、附近文字），并直接列出与母版不一致的字段与一键修复入口。
- 填报页关闭守卫：改动后点弹窗外面/按 Esc/点关闭会**先自动保存为草稿再关闭**；保存失败则不关闭并给出"放弃修改"出口。
- 刷新/关闭标签页时对未保存的弹窗内容给出浏览器原生提醒。

### 本次修复的缺陷（相对开发期实现）

| 缺陷 | 影响 | 修复 |
| --- | --- | --- |
| 客户端回传"金额大写"计算值 | 保存/生成必然 422 | 后端始终按来源重算，忽略客户端值 |
| 布尔字段无法填报（字符串 vs 布尔） | 该类型字段永远填不进 | 兼容 `true/false/是/否` 与布尔值 |
| 只兼容 H2 的 SQL（`MERGE … KEY`、`NEXT VALUE FOR`） | PostgreSQL 上语法错误 | 改为 UPDATE+INSERT upsert 与 `NEXTVAL` |
| PDFBox 取字典名抛 NPE | **所有 PDF 被拒、无法生成** | 改为 `getDictionaryObject` + 类型判断 |
| 占位符落在横向合并单元格被拒 | 常见版式无法作为母版 | 只拒绝纵向合并（`vMerge`） |
| 生成任务重复、快照复用判断失效 | 重复生成、冗余文件 | 修订复用检查（重复生成返回 `reused`） |
| 一键启动脚本为 UTF-8 | cmd 按 GBK 解析导致命令被吞、启动失败 | 脚本改为纯 ASCII |
| 用 `pg_isready` 判断数据库就绪 | 首次初始化时误判，应用启动失败 | 改为实际连接一次数据库 |
| 版本/模板软删除后无法清理 | 废弃数据长期残留 | 改为物理删除 + 引用回收 + 审计 |

### 代码评审后的加固（同日）

外部代码评审提出 6 条，逐条核对后确认 3 条属实、3 条不成立（详见提交说明）。已修复：

| 项 | 说明 |
| --- | --- |
| 渲染配置标识写死 | `contract_revisions.render_profile_id` / `review_receipts.render_profile_id` 原先硬编码 `libreoffice-noto-cjk-v1`，现在统一取 `app.render-profile`（`GenerationService.renderProfile()`），换渲染环境后审计与确认凭据记录的值不再说谎 |
| `SCAN_COMMAND` 不支持带参数 | 原实现把整串当成可执行文件名（`new ProcessBuilder(scanCommand, file)`），配 `clamscan --no-summary` 这类命令必然失败；现按"可执行文件 + 参数"解析（支持引号内空格），并在程序无法启动时返回 422 `FILE_SCAN_FAILED` + 明确提示，而不是 500 |
| 未就绪状态下的提示不可操作 | 试填/生成未成功时，原先由 `Db.one` 抛出通用 404「记录不存在或无权访问」；现改为 409 + `TEST_NOT_READY` / `PREVIEW_NOT_READY` / `REVIEW_OUTDATED`，直接告诉用户"请先试填/先生成全文/重新确认" |
| 分页参数越界 | `page` 原只有下限；`page=2147483647` 会让 `(page-1)*size` 溢出成负 OFFSET → 数据库报错（500）。现三个列表接口都夹紧到 `1 ≤ page ≤ 1000000`、`size ≤ 100/200`，并用测试固化 |

新增 `scripts/test-api-guards.cjs`：不需要浏览器，用内存库起一个真实实例，把上面这些行为（404 而不是 500、409 提示、分页夹紧、带参数的扫描命令）全部断言一遍，共 19 项检查（含 3 项界面样式断言）。

### 界面（同日）

- **换成浅色主题**：白色卡片 + 浅灰蓝底、蓝色强调色、圆角与极轻阴影；导航改为胶囊高亮，统计卡加彩色强调条，表格表头浅底 + 行悬停，状态徽标加状态圆点，弹窗头部吸顶，表单控件统一聚焦蓝环。
- **按钮位置**：弹窗底部第一个按钮（关闭/取消）靠左、其余靠右，按钮自动换行不再挤压；工具条与分页按钮统一换行规则。
- **响应式**：≤1000/760/560px 三档自适应（统计卡两列→一列、表单单列、小屏收起导航）。
- 修复：定稿/已签署的合同上不再显示"草稿在生成后被修改过，请重新生成后再定稿"——该提示只对草稿有意义（定稿时 `lock_version` 必然递增），否则会在正式合同上误导用户。
- 新增 `docs/images/` 界面截图，README 增加「界面预览」一节。

---

## 后续计划（未实现）

- `TIMESTAMP` → `timestamptz`（跨时区部署前）
- PDF 预览中的字段位置高亮、在预览里框选生成字段
- 批量把历史模板字段名中文化
- 除"颜色/下划线"之外的更多自动识别（如空括号、表格空单元格）

如果你希望某项优先做，请在 Issue 里描述使用场景。
